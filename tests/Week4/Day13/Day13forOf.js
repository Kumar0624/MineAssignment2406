let courses = ["Playwright","Selenium","Manual"];

 for (let tempVal of courses ){ 
     /* tempval = Playwright
    tempVal = Selenium
    tempVal = Manual  */
    console.log(tempVal);
 }

 courses.forEach( (val)   => {
    console.log(val);
    
 });